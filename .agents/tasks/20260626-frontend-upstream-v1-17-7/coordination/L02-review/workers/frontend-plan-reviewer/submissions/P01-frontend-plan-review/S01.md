Review Status
- workflow.operation.name: frontend_plan_review
- workflow.operation.status: completed
- workflow.review_scope.status: partial
- workflow.scope_check.status: requirements_missing
- workflow.findings.status: high_risk_found
- verdict: changes_requested

Review Scope

只读审查 `dev <- main@acf432b` 前端吸收计划、提交清单、冲突策略和 fork 定制保护。已读取 `CLAUDE.md`、`task-charter.md`、`findings.md`、实施计划，以及 7 个候选冲突文件在 `dev` 与 `main` 的相关差异。未修改文件，未执行 merge / commit / push / tag / release / install。

Scope Check

提交清单覆盖 `git log --reverse --oneline dev..main` 的 27 个提交，数量与 Author Claim 一致。7 个已知冲突文件均被计划点名，但审查发现一个关键相邻文件 `src/features/authFiles/uiState.ts` 同时被 `dev` 和 `main` 修改，且承担 `statusFilterMode`、旧 boolean 状态和批量检查并发持久化的类型契约，当前计划没有把它作为明确合并点。`git merge-tree --write-tree --name-only dev main` 因只读文件系统失败，冲突文件集合无法在本轮独立复核为“仅 7 个”。

Findings

F01
Severity: high
Summary: AuthFiles 状态过滤迁移计划漏掉 `uiState.ts`，且 `enabledOnly` 旧状态兼容没有可执行规则。
Evidence: `findings.md` 只在 `AuthFilesPage.tsx` 策略里写“读取旧 problemOnly / disabledOnly / enabledOnly 字段兼容”，但验证只要求 previous `problemOnly` / `disabledOnly` state；`dev:src/features/authFiles/uiState.ts` 有 `enabledOnly` 和 `batchCheckConcurrency`，`main:src/features/authFiles/uiState.ts` 有 `AuthFilesStatusFilterMode/statusFilterMode` 但没有 `enabledOnly/batchCheckConcurrency`；`main:src/pages/AuthFilesPage.tsx` 的旧状态推导只处理 `problemOnly/disabledOnly`。
Impact: 进入合并时容易出现类型契约漏合并，或者老用户的 enabled-only 筛选持久化被静默降级为 all；这正好落在 AuthFiles 高风险冲突面。
Recommendation: 在计划和 conflict strategy 中显式加入 `src/features/authFiles/uiState.ts`，要求合并后同时保留 `statusFilterMode`、`enabledOnly`、`batchCheckConcurrency`、`isAuthFilesStatusFilterMode`，并定义旧状态迁移优先级：problem > disabled > enabled > all。
Confidence: high

F02
Severity: medium
Summary: Fork 定制验证不完整，ZIP 下载和 release policy 没有可执行核查。
Evidence: `CLAUDE.md` 将 ZIP 下载、tag-only release、fork 版本后缀列为不可丢定制；`findings.md` 的 Verification Notes 只列 DisplayName、AuthFiles 状态卡、Scoped Poll 和 batch controls，没有 ZIP 和 release policy；`git diff --name-status dev..main -- .github scripts release-metadata.env package.json` 显示 release 相关文件有删除/修改风险，包括 `.github/workflows/release.yml`、`release-metadata.env`、`scripts/version.sh` 等；ZIP 路径在 `useAuthFilesData.ts` 调用 `authFilesApi.downloadArchive`，但计划没有要求验证该路径。
Impact: 即使 type-check/lint/build 通过，也可能丢掉批量 ZIP 下载或 release 后缀/tag-only 策略，属于 fork 定制回归。
Recommendation: 在 L03/L04 验证清单加入静态 release policy 检查：`release.yml` 只允许 tag 发布且 job 受 tag guard 约束、`scripts/version.sh`/`release-metadata.env` 的 fork 后缀逻辑保留；加入 ZIP 验证：选择多个 auth files 时仍走 `/auth-files/download-archive` 并生成 `.zip` 文件名。
Confidence: high

F03
Severity: medium
Summary: “已知 merge conflicts 仅 7 个文件”的核心证据本轮无法复核。
Evidence: 按要求执行 `git merge-tree --write-tree --name-only dev main`，结果为 `error: unable to create temporary file: Read-only file system` / `fatal: failure to merge`。
Impact: L03 若直接依赖旧 rehearsal 清单，可能在合并时遇到额外冲突或自动合并误伤，尤其是 `uiState.ts` 这类相邻契约文件。
Recommendation: 在代码合并前，用可写的隔离 worktree 或 coordinator 环境重新运行同一条 `merge-tree` 证据命令并记录输出；如果冲突集合超出计划，先回 L02 修正文档，不进入业务代码合并。
Confidence: medium

Scorecard
Scope Control: 3
Evidence Quality: 3
Correctness: 3
Safety: 3
Testability: 2
Maintainability: 3

Verification Evidence

- `git log --reverse --oneline dev..main` 返回 27 个提交，从 `b566884 feat(quota): show Codex reset credit expiries` 到 `acf432b feat: update trackWrapper styles for improved thumb positioning`。
- `git merge-tree --write-tree --name-only dev main` 在只读沙盒失败，未产生冲突文件输出。
- `git merge-base dev main` 为 `ed4124ff3b24f526b0a84003451121794c14d9bd`。
- `git diff $base..main` 与 `git diff $base..dev` 均显示 7 个候选冲突文件外加 `src/features/authFiles/uiState.ts` 被双方修改。
- 已读取 `dev` 与 `main` 的 `AuthFilesPage.tsx`、`ProviderResourceTable.tsx`、`ResourceDetailView.tsx`、`config.ts`、`transformers.ts` 相关片段。

Open Questions / Limitations

- 未联网 fetch，未重新核验 `origin/main == upstream/main`，只基于任务文件声明和本地 refs 审查。
- 未执行真实 merge、测试、构建或浏览器验证。
- 未写入 `S01.md`，因为本 packet 的 Write Scope 为 None，且未收到 coordinator 明确写入请求。

Recommended Next Step

先修订计划：补 `uiState.ts` 合并策略、enabled-only 迁移规则、ZIP/release policy 验证项；然后在可写隔离环境重跑 `merge-tree --write-tree --name-only dev main`。这些更新完成前，不建议进入 L03 代码合并。

