Review Status
- workflow.operation.name: P02-frontend-verification-review
- workflow.operation.status: completed
- workflow.review_scope.status: in_scope
- workflow.scope_check.status: pass
- workflow.findings.status: issues_found
- verdict: changes_requested

Review Scope

只读审查了候选计划、`CLAUDE.md`、`package.json`、现有脚本/测试入口，以及指定的前端 UI/API 文件。未修改文件，未安装依赖，未运行 build/test，未读取凭证。

Scope Check

Write Scope: None 已遵守。未触发 push/tag/release/upload，也未触发长耗时构建。必要文件均可读取，审查未依赖外部凭证或未授权资源。

Findings

ID: F-01
Severity: High
Summary: L04 验证计划仍以命令为主，缺少可执行的 fork 定制人工验收步骤和通过标准。
Evidence: `package.json` 只有 `type-check`、`lint`、`build`、`dev`、`preview` 等脚本，未提供测试脚本；计划 Task 4 只列 `bun run type-check/lint/build`。`CLAUDE.md` 要求每次同步验证 5 项 fork 定制。
Impact: typecheck/lint/build 不能证明 DisplayName、Auth Files Batch Check、Scoped Poll、ZIP 下载、release policy 在合并后仍按 fork 行为工作。
Recommendation: L03 前补充 L04 checklist：启动 `bun run dev` 或 `bun run preview` 的条件、页面路径、测试数据/本地存储准备、每项定制的操作步骤、观察断言、失败时停止条件。
Confidence: High

ID: F-02
Severity: High
Summary: Auth Files Batch Check 和 Scoped Poll 的手动检查依赖运行时数据/API，但计划没有定义后端前提、无凭证路径或降级规则。
Evidence: `AuthFilesPage.tsx` 中批量检查依赖 `useAuthFilesBatchCheck`、scope 为 `selected/page/filtered`，并展示 async job/progress/results；Scoped Pool 摘要依赖 `config.routingStrategy` 与 scoped pool runtime 数据。计划只写“真实后端状态”为停止条件，没有说明如何在无外部凭证下验证。
Impact: L04 可能在没有可用后端或测试数据时把代码检查误当行为验证，导致“吸收成功”结论过强。
Recommendation: 明确两档结果：有本地非敏感后端/测试数据时做行为验收；没有时标记 blocked 或 partial，不得声明 fork 定制行为已验证。
Confidence: High

ID: F-03
Severity: Medium
Summary: ZIP 下载被列为必须保留，但当前验证说明没有覆盖它。
Evidence: `CLAUDE.md` 明确列出“认证文件多选压缩下载”；`AuthFilesPage.tsx` 中存在批量选择后 `batchDownload(selectedNames)` 的入口。`findings.md` Manual checks 只提 DisplayName、旧状态过滤、Scoped Poll 和 batch controls mobile。
Impact: ZIP 下载可能在合并中丢失或按钮可见但下载失败，现有计划无法发现。
Recommendation: 在 L04 加入认证文件列表选择两项以上、触发批量下载、确认 zip 文件生成/文件名/内容清单的手动检查；若浏览器下载无法验证，至少记录受限原因。
Confidence: High

ID: F-04
Severity: Medium
Summary: release policy 验证口径存在文档冲突，计划没有要求核对 workflow 事实。
Evidence: `CLAUDE.md` 声称“仅 tag 触发正式发布”；`.github/workflows/release.yml` 的 job 条件是 `startsWith(github.ref, 'refs/tags/v')`；但 `docs/fork-maintainer-workflow.md` 仍写“Every push to master now triggers release”。
Impact: L03/L04 后续推进到 master 评估时，操作者可能被旧文档误导，错误判断 push master 的外部副作用。
Recommendation: L04 release policy 检查应显式核对 `.github/workflows/release.yml` 触发条件，并记录 `docs/fork-maintainer-workflow.md` 的 stale/conflicting 文字为后续文档修正项。
Confidence: Medium

ID: F-05
Severity: Medium
Summary: 依赖一致性验证没有进入主计划测试列表。
Evidence: `packageManager` 是 `bun@1.3.14` 且仓库存在 `bun.lock`；release workflow 使用 `bun install --frozen-lockfile`。`findings.md` 备注了依赖状态变化时运行 frozen lockfile install，但实施计划 Task 4 的测试列表没有包含这个条件。
Impact: upstream merge 若改动 `package.json` 或 `bun.lock`，直接 typecheck/build 可能使用本地旧依赖状态，验证结论不稳定。
Recommendation: L04 加入条件门禁：若 `package.json` 或 `bun.lock` 在 merge 后变化，或 node_modules 状态不可确认，先运行 `bun install --frozen-lockfile`；若不能运行，降级并披露风险。
Confidence: Medium

Scorecard
Scope Control: 4
Evidence Quality: 3
Correctness: 2
Safety: 4
Testability: 2
Maintainability: 3

Verification Evidence

- 读取 `CLAUDE.md`：确认 5 项不可丢失定制和每次同步必须验证。
- 读取 `package.json`：确认无 test/e2e 脚本，核心验证脚本为 `type-check`、`lint`、`build`。
- 读取实施计划与 `findings.md`：确认当前命令验证明确，但人工检查缺少完整步骤/断言。
- 检索现有测试文件：未发现 test/spec runner 配置或测试文件。
- 读取 `AuthFilesPage.tsx`、`ProviderResourceTable.tsx`、`ResourceDetailView.tsx`、`config.ts`、`transformers.ts`：确认指定定制有代码入口，但需要运行时行为观察才能证明未回归。
- 读取 `.github/workflows/release.yml` 与 fork workflow 文档：确认 tag-only workflow 与文档描述存在冲突。

Open Questions / Limitations

- 未运行 dev server 或浏览器人工验收，因为本 packet 禁止长运行/执行面操作。
- 未验证真实后端数据可用性；若 L04 需要真实批量检查结果，必须先确认无凭证、无外部副作用的本地验证路径。
- 当前结论是 plan review，不是代码合并后的行为验证结论。

Recommended Next Step

先修订 L03/L04 计划：把 5 项 fork 定制转换成可执行手动验收清单，并明确 dev/preview server、后端前提、依赖安装门禁、release workflow 核对和无法验证时的降级/阻断规则。修订后再进入 L03 合并。

