# 前端方案独立复评 S01

- reviewer: Hypatia / 019f6536-58ab-7e32-9a93-f3d2124a28a6
- mode: read-only
- status: changes_requested
- files_touched: none

## Disposition 核验

- Round 1 的 xAI adapter、完整 tests、30-path ledger、main mirror、master candidate、batch quota、Scoped Poll、ZIP、Usage v2 和清单错误均确认 fixed。
- API Key editor reopen，见 R2-H01。
- 仓库规则与显式 skip 新增 finding，见 R2-H02。

## R2-H01 API Key reveal 的 resolved shape 仍会返回空值

- Severity: High。
- Evidence: `entry.apiKey` 是必填 string，现有记录初始化为 `''`；`entry.apiKey ?? entry.existingApiKey` 永远选择空字符串，也不能区分未编辑与主动清空。
- Recommendation: 使用独立 edited-state；仅 reveal、未编辑且 apiKey 为空时显示 existingApiKey，首次输入或清空后始终使用 apiKey。
- Disposition: fixed。

## R2-H02 仓库规则、分支策略和 skip 边界不一致

- Severity: High。
- Evidence: ignored `CLAUDE.md` 含 master-first 旧说明和不存在的 docs/upstream-sync-plan.md；方案使用 dev-first 且跳过上游 AGENTS.md，但此前未显式列为例外和用户确认项。
- Recommendation: 明确 tracked `.agents/README.md` 与用户规则为权威；ignored CLAUDE 仅作功能参考；将 AGENTS.md 列为唯一显式 skip、候选门禁和用户确认项。
- Disposition: fixed。

## 结论

- 新增/重开 finding: High 2。
- Verdict: changes_requested。
- 修订后需执行聚焦 Round 3 只读复核。
