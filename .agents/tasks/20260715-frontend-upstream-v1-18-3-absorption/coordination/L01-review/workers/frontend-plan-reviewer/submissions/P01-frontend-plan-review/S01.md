# 前端方案独立评审 S01

- reviewer: Hypatia / 019f6536-58ab-7e32-9a93-f3d2124a28a6
- mode: read-only
- status: changes_requested
- files_touched: none

## 范围与证据

- 核对本地规则、任务材料、固定 base/dev/master、38 个提交、merge-tree、fork 历史定制及 release workflow。
- 固定目标 d3df9b07 / v1.18.3；增量 38 commits、141 files、+4,766/-4,450。
- merge-tree e04732405bda28474bcf9ae19a522e34ded256b4；13 个机械冲突和 17 个自动合并热点。
- 未执行 checkout、merge、install、test、commit、push 或写入。

## Findings

### H-01 xAI 新额度语义会被 fork adapter 静默丢弃

- Evidence: fork 的 xaiStateToQuotaView 仍只生成 monthly 行；上游 quotaConfigs 已渲染 weekly、product usage、on-demand 和 monthly。
- Impact: Auth Files 单文件额度路径不会展示 v1.18.3 的 xAI 新数据，type-check 无法发现。
- Recommendation: 把 xaiStateToQuotaView 列为明确解决项，增加 weekly/product/on-demand/monthly fixture。
- Disposition: fixed。

### H-02 Provider 冲突策略未覆盖新 API Key 编辑器

- Evidence: 冲突预检只指定 BaseProviderForm；上游新增 ApiKeyEntriesEditor，现有输入值只取 entry.apiKey，reveal 时不会显示 existingApiKey。DisplayName 还跨 form、types、adapters、provider API 并发更新链。
- Impact: OpenAI compatibility 多 Key 的已保存 Key 回显会为空；DisplayName 保存、清空或并发保留也可能回归。
- Recommendation: 将新 editor 和 provider 数据链纳入切片；验证单 Key、多 Key、未编辑保存、编辑、删除，以及 DisplayName create/edit/clear。
- Disposition: fixed。

### H-03 全量验证未运行上游新增测试套件

- Evidence: 方案仅要求 usage tests；上游新增 test/verify 脚本及 16 个 tests/*.test.ts，预合并树共有 19 个测试文件。
- Impact: provider/config 并发、quota cache、plugin、OAuth、official API 等回归测试可能完全未执行。
- Recommendation: frozen install 后运行 bun run test 和 bun run verify；test:usage 作为专项门禁保留。
- Disposition: fixed。

### H-04 17 个自动合并热点未形成可确认清单

- Evidence: 报告只记录数量；热点包括 quotaConfigs.ts、providers.ts、useProviderWorkbench.ts、authFiles.ts、VisualConfigEditor.tsx、MainLayout.tsx 等。
- Impact: 用户确认 13 个机械冲突不足以确认完整吸收策略。
- Recommendation: 补全 17 个文件及 auto-accept、semantic-review 或 explicit-fix 裁决和对应验证。
- Disposition: fixed。

### H-05 缺少 origin/main 与 upstream/main 对齐门禁

- Evidence: CLAUDE.md 明确要求同步前对齐；当前 origin/main@fd22c14、upstream/main@d3df9b0，治理方案未列 mirror 同步或例外。
- Impact: 违反仓库同步规则，后续自动同步与本次 dev merge 可能采用不同基线。
- Recommendation: 在独立授权点对齐并验证远端 main，或记录经确认的分支策略例外。
- Disposition: blocked，需在用户确认清单中明确选择。

### H-06 dev 到 master 的推进方式未定义

- Evidence: master...dev 为 11/9，merge base cfabc797；方案只要求仅代码进入 master，没有候选树构造方法。
- Impact: 直接 merge 可能带入 .agents；其他方式可能漏提交、改变 ancestry，或让 tag 不指向实际验证树。
- Recommendation: 固定 master candidate 构造步骤，比较业务树与已验证 dev candidate，确认 .agents 为空并在该 SHA 重跑门禁。
- Disposition: fixed。

### H-07 batch quota 已知回归没有持久测试门禁

- Evidence: 历史修复依赖一次性 SSR 样本；d3df9b0 正在修改同一 Codex additional-window 分类。
- Impact: 月度误标五小时、空 weekly 行可能再次出现。
- Recommendation: 增加 Bun 回归测试，覆盖旧 five-hour ID 配月度时长、空 weekly、additional primary/secondary 互换及 A/B parity。
- Disposition: fixed。

### M-01 Scoped Poll 验证矩阵不具体

- Recommendation: 增加 YAML round-trip、最新快照合并、开关/策略组合、unknown fields、image passthrough 并发保存和 AuthFileCard badge smoke。
- Disposition: fixed。

### M-02 ZIP 仅有静态保留证据

- Recommendation: 验证请求去重、跨页选择、Content-Disposition、失败反馈及 mobile 可达性。
- Disposition: fixed。

### M-03 Usage v2 缺少页面级验证

- Evidence: normalization/cost 测试不能证明 /usage 导航、Chart.js 渲染及详情弹窗可用。
- Recommendation: 使用代表性 v2 fixture 增加浏览器 smoke。
- Disposition: fixed。

### L-01 提交矩阵存在一处错误和欠分类

- Evidence: official API 提交真实短 SHA 为 07562b7，清单误写为 0758915；4af4cf4 同时新增 AGENTS.md、修改 README/package，不只是 CI。
- Recommendation: 修正 SHA 和分类。
- Disposition: fixed。

## 结论

- Critical: 0
- High: 7
- Medium: 3
- Low: 1
- Verdict: changes_requested
- Next: 修订 inventory、自动合并热点、额度/provider 策略、完整测试及 main/master/tag 边界后进行独立复评；复评通过前不得进入 L02。
